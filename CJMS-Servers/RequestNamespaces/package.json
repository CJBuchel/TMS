{
  "name": "@cjms_servers/request_namespaces",
  "version": "1.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.6.2"
  },
   "scripts": {
    "copy-env-file": "copyfiles -u 1 ../../.env ./.env",
    "clean": "rimraf lib/",
    "build": "yarn run clean && tsc",
    "build-dev": "tsc -w"
  }
}
