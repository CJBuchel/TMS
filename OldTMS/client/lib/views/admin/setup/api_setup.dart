import 'package:flutter/material.dart';

class APISetup extends StatelessWidget {
  const APISetup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Theme.of(context).secondaryHeaderColor,
      child: const Center(
        child: Text('API Setup (Coming Soon)'),
      ),
    );
  }
}
