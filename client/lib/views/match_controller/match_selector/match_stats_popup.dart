import 'package:flutter/material.dart';

class MatchStatsPopup extends StatelessWidget {
  const MatchStatsPopup({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Match Statistics'));
  }
}
