import 'package:flutter/material.dart';

class RefereeScoringHandler extends StatelessWidget {
  const RefereeScoringHandler({Key? key}) : super(key: key);

  @override
  build(BuildContext context) {
    return Container();
  }
}
