import 'package:flutter/material.dart';

class JudgeInfo extends StatefulWidget {
  const JudgeInfo({Key? key}) : super(key: key);

  @override
  State<JudgeInfo> createState() => _JudgeInfoState();
}

class _JudgeInfoState extends State<JudgeInfo> {
  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 160, child: Card(child: Column(children: [])));
  }
}
