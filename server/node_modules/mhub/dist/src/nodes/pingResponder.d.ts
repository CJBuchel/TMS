import Message from "../message";
import * as pubsub from "../pubsub";
export declare class PingResponder extends pubsub.BaseSource {
    send(message: Message): void;
}
export default PingResponder;
