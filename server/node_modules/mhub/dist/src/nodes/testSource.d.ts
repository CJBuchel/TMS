import * as pubsub from "../pubsub";
export interface TestSourceOptions {
    topic?: string;
    interval?: number;
}
export declare class TestSource extends pubsub.BaseSource {
    constructor(name: string, options?: TestSourceOptions);
}
export default TestSource;
