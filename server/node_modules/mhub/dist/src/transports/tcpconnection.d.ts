/**
 * Connect TCP socket to an MServer hub.
 */
/// <reference types="node" />
import * as net from "net";
import Hub from "../hub";
export declare class TcpConnection {
    private _socket;
    private _name;
    private _client;
    private _buffer;
    constructor(hub: Hub, socket: net.Socket, name: string);
    private _handleClientResponse;
    private _handleSocketClose;
    private _handleSocketError;
    private _handleProtocolError;
    private _handleSocketData;
}
export default TcpConnection;
