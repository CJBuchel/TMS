import 'package:flutter/material.dart';

class ScoringOverview extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Scoring overview"),
    );
  }
}
