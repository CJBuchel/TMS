import 'package:flutter/material.dart';

class EditMatchTables extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Edit Match Tables'),
    );
  }
}
