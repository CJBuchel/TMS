import 'package:flutter/material.dart';

class TimerControls extends StatelessWidget {
  const TimerControls({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // color: Colors.blue,
      child: const Center(
        child: Text('Timer Controls'),
      ),
    );
  }
}
