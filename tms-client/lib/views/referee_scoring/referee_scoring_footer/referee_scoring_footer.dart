import 'package:flutter/material.dart';

class RefereeScoringFooter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40,
      decoration: const BoxDecoration(
        color: Colors.green,
      ),
      child: const Text('Referee Scoring Footer'),
    );
  }
}
